
  # TeamWorks Program

  This is a code bundle for TeamWorks Program. The original project is available at https://www.figma.com/design/WnCcroU6o3vthh6rOL02IC/TeamWorks-Program.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  